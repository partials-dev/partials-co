{
  "name": "partials-co",
  "version": "2.0.0",
  "private": true,
  "devDependencies": {
    "firebase-tools": "^3.6.1",
    "fs-extra": "^3.0.1",
    "glob": "^7.1.1",
    "lodash": "^4.17.4",
    "react-scripts": "1.0.5",
    "react-snapshot": "^1.0.4",
    "sharp": "^0.17.3"
  },
  "dependencies": {
    "@hughsk/fulltilt": "^0.7.1",
    "canvas-prebuilt": "^1.6.5-prerelease.1",
    "classnames": "^2.2.5",
    "dotenv": "^4.0.0",
    "pixi.js": "4.5.2",
    "preact": "^8.1.0",
    "preact-redux": "^2.0.1",
    "preact-render-to-string": "^3.6.1",
    "preact-router": "^2.5.2",
    "promise-latest": "^1.0.4",
    "redux": "^3.6.0",
    "sw-toolbox": "^3.6.0"
  },
  "scripts": {
    "start": "npm run resizeImages && react-scripts start",
    "build": "react-scripts build && npm run resizeImages && react-snapshot && node asyncScriptTagTransform.js && node precacheServiceWorker.js",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && firebase deploy",
    "resizeImages": "node resizeImages.js"
  }
}
