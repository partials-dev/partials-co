{
  "name": "partials-co",
  "version": "2.0.0",
  "private": true,
  "devDependencies": {
    "amphtml-validator": "^1.0.20",
    "firebase-tools": "^3.6.1",
    "react-scripts": "0.9.5",
    "react-snapshot": "^1.0.4"
  },
  "dependencies": {
    "react": "^15.5.4",
    "react-dom": "^15.5.4",
    "react-helmet": "^4.0.0",
    "react-router-dom": "^4.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && react-snapshot && node precacheServiceWorker.js",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && firebase deploy"
  }
}
